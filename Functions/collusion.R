# community collusion

collusion <- function(forage_locs_list, prob_gauntlet_seal_i, alpha){
  P_social <- (1-alpha) * prob_gauntlet_seal_i + alpha * mean(forage_locs_list[,t,j])
  return(P_social)
}
